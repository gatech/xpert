var stack = new Array(); stack.push("index");function pushStack(newVal) {   alert("Pushing" +newVal);   // index screen will always be there in Stack   if(newVal == "index")     return;   stack.push(newVal);}function popStack() {   var popVal = stack.pop();   if (popVal == undefined) {            return "Nothing left!";	}   else {      // index screen should always been in the stack      if(popVal == "index")	    pushStack("index");      return popVal;      }         }function showStack(theSelect){   theSelect.options.length = 0;   for (var i = 0; i < stack.length; i++){      var theOption = new Option(stack[i]);      theSelect.options[theSelect.options.length] = theOption;   }}